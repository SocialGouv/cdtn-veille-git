import { ComponentEnvironment } from "../types";

const env: ComponentEnvironment["veille"] = {
  imageTag: "v0.0.0", // renovate: docker~metabase/metabase
  subdomain: "veille-git.",
};

export default env;
